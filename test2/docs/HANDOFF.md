# Project handoff — integration / QA

## Integration / QA — 2026-09-23

Сквозная демонстрация проверена на работающих FastAPI:8000 и Vite:5173 через
Google Chrome. Серверы перезапущены с актуальным кодом и оставлены работающими.
Архитектура сохранена; косметического рефакторинга нет.

### Исправленный баг

При быстрой последовательности «применить тему → выбрать готовность» второй
фильтр мог стереть тему из URL. В React Router переход уже менял адрес, но обработчик
следующего поля ещё видел старый `search` из предыдущего render. `Catalog.filter`
теперь объединяет изменения с текущим URL. Регрессионный сценарий воспроизвёл
потерю фильтра до правки, после исправления и reload находит ровно нужную задачу.
Файл: `frontend/src/pages/Explore.tsx`.

### Результат проверок

| Проверка | Итог |
| --- | --- |
| `.venv/bin/python -m pytest backend/tests -q` | **70 passed**, 0.95 s |
| `cd frontend` → `npm run test:e2e` | **6 passed**, 20.9 s |
| `cd frontend` → `npm run build` | TypeScript + Vite успешно |
| Реальный UI: бизнес → команда → бизнес | Все 16 шагов пройдены |
| Rating | 10 → 50/workable → 70/ready; отдельно 100 → 80 и публикация при 0 |
| Readiness | Проверены веса и границы 0/39/40/69/70/89/90/100 |
| Каталог | Обе сортировки по рейтингу, newest, пагинация; topic + readiness вместе |
| Формы | Пробелы, обязательные поля, неправильный URL, пустой необязательный URL |
| API errors | 404/409/422, сетевой отказ, искусственный 500; повтор и сохранность ввода |
| AI errors | Неверный JSON/схема/источники, timeout/429/500/отказ; fallback без падения |
| Frontend console | Нет console error, pageerror и API 5xx в обычном полном сценарии |
| Backend exceptions | В логах финального прогона нет traceback, ASGI exception и HTTP 5xx |
| SQLite | integrity_check=ok, foreign_key_check=[] |

Persistence проверена через reload описания, ответов, несохранённых правок редактора,
сохранённого рейтинга, подтверждения, роли, фильтров, формы предложения и принятого
решения. Backend-тесты дополнительно закрывают приложение/engine и открывают ту же
SQLite-базу заново: опубликованная карточка и accepted-предложение сохраняются.

Новые регрессионные проверки: `frontend/tests/integration.spec.ts` и
`backend/tests/test_integration_qa.py`. Первый сценарий работает с настоящим API;
отказы 422/500 для проверки UI вводятся перехватом только в соответствующем тесте.
Ожидаемые browser network errors при таких отказах не считаются сбоем обычного сценария.
Сбой AI проверен через внедрённый некорректный провайдер и HTTP MockTransport;
живой внешний LLM с ключом не вызывался. По умолчанию сервер работает с mock.

Лог актуального backend: `/tmp/hackathon-qa-backend.log`. Перед seed сохранена
копия рабочей SQLite: `database/app.pre-qa.db` (исключена из Git).

### Воспроизводимые seed-данные

```bash
.venv/bin/python -m database.seed
```

Чистая БД получает **8 задач** (2 draft, 1 confirmed, 5 published),
**5 профилей команд**, **5 предложений** (3 pending, 1 accepted, 1 rejected).
У опубликованных задач представлены все четыре уровня: 10, 45, 70, 100 баллов.
Принятый seed-отклик — явно заданная фикстура, не автоматический выбор.

Профили из `database/team_profiles.json`:
Data Bakers, Charts Lab, Campus Team, Discovery Team, Attendance Lab.
Каждый содержит name, description, skills и contact. Они сохраняются в отдельной
таблице `team_profiles`, создаваемой при startup/seed без изменения старых таблиц,
и доступны через **GET /api/teams** (limit/offset, сортировка id ASC).
Профили — демонстрационный справочник, не аккаунты. Формы предложений продолжают
принимать свободные team_name/contact; регистрации и автоматической привязки нет.

Первый seed на рабочей базе добавил 5 профилей; повторный вернул:
`{"added_tasks":0,"added_proposals":0,"added_teams":0}`. Тест подтвердил, что
повторный seed не перезаписывает правки задачи и профиля.

После всех QA-прогонов рабочая БД содержит 21 задачу, 5 профилей, 23 предложения.
Это больше чистого seed: браузерные тесты добавляют свои записи и не удаляют их.
Для новой демонстрации можно использовать отдельный DATABASE_URL с новым SQLite-файлом;
существующую БД удалять не требуется.

### Последняя готовая демонстрация

- `http://127.0.0.1:5173/tasks/21` — опубликованная задача мастерской, 70 / ready.
- `http://127.0.0.1:5173/tasks/21/proposals` — предложение Integration Team принято
  вручную; результат сохраняется после reload.
- `http://127.0.0.1:5173/tasks/20` — опубликованная задача с 0 баллов и предложением.
- `http://127.0.0.1:8000/api/teams` — пять профилей команд.

ID относятся к текущей локальной БД. Для показа с нуля: Business → создать задачу
→ три ответа → сохранить правку → подтвердить → опубликовать → Student → каталог
→ карточка → отправить предложение → Business → предложения → принять → reload.

Команды запуска из корня, в двух терминалах:

```bash
.venv/bin/python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

```bash
cd frontend
npm run dev
```

Повторно запускать не нужно, если оставленные серверы уже слушают эти порты.
Для чистой установки зависимости: `python -m pip install -r backend/requirements.txt`
в активированном `.venv` и `npm ci` внутри frontend.

---

Ниже — сведения предыдущих этапов; актуальные QA-результаты и demo-ID указаны выше.


Обновлено: 2026-09-23. Реализован React + Vite frontend для демонстрации
сквозного сценария. Backend, SQLite и API в frontend-сессии не изменялись.
Рабочее название интерфейса — «Практика»; язык — русский.

## Frontend: что готово

- Dashboard с двумя направлениями: бизнес и студенческая команда; переключатель
  демо-роли в шапке, без регистрации.
- Создание из короткого текста, результат анализа и три вопроса из API.
  Mock/fallback честно обозначен как базовый/резервный анализ.
- Редактор всех десяти полей и темы; справа серверный рейтинг 0–100,
  breakdown и рекомендации. После кнопки «Сохранить» рейтинг обновляется.
  При несохранённых правках показано предупреждение; расчёта баллов на клиенте нет.
- Подтверждение и публикация — отдельные действия. Нулевой рейтинг разрешён.
- Каталог с серверной сортировкой, точным фильтром по теме, фильтром готовности
  и пагинацией. Высокий рейтинг выделен, низкий остаётся видимым.
- Полная карточка, форма предложения, список предложений бизнеса, ручные
  принятие/отклонение с подтверждением. Автоматического назначения нет.
- Loading skeletons, ошибки с повтором, пустые состояния, восстановление текста
  форм через localStorage, адаптивная боковая навигация, keyboard focus и reduced motion.
- Все карточки, показатели и предложения поступают из API. В компонентах нет
  mock-массивов. Примеры внутри placeholder не являются данными каталога.

### Файлы frontend

- `frontend/src/api.ts`, `types.ts` — типизированный API client, таймаут и ошибки.
- `frontend/src/workspace.tsx`, `hooks.ts` — роль, загрузка каталога/счётчиков,
  локальные ссылки на созданные задачи, обработка запросов.
- `frontend/src/App.tsx`, `components.tsx` — оболочка, маршруты, общие состояния,
  карточки, серверный рейтинг.
- `frontend/src/pages/Explore.tsx` — dashboard, каталог, рабочие задачи.
- `frontend/src/pages/Intake.tsx` — описание, уточнение, редактор.
- `frontend/src/pages/Task.tsx` — детали, форма и рассмотрение предложений.
- `frontend/src/styles.css` — типографика Manrope, светлые поверхности, зелёные
  акценты, адаптивность. Шрифт поставляется локально через npm, без CDN.
- `frontend/vite.config.ts`, `.env.example` — proxy и адрес API.
- `frontend/tests/demo.spec.ts`, `playwright.config.ts` — браузерный сквозной тест.
- `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`,
  `.impeccable/surfaces/frontend-src-app-tsx.md` — контекст продукта
  и выбранное направление интерфейса; изображения проверки — `.impeccable/review/`.

### Маршруты интерфейса

| URL | Экран |
| --- | --- |
| `/` | Dashboard |
| `/catalog` | Каталог |
| `/drafts` | Задачи, созданные в этом браузере |
| `/tasks/new` | Описание задачи |
| `/tasks/:id/questions` | Анализ и уточняющие вопросы |
| `/tasks/:id/edit` | Редактор и рейтинг |
| `/tasks/:id` | Полная карточка |
| `/tasks/:id/propose` | Предложение команды |
| `/tasks/:id/proposals` | Рассмотрение предложений |

### Запуск frontend

Backend должен быть запущен на `http://127.0.0.1:8000` по инструкции ниже.
Во втором терминале из корня проекта:

```bash
cd frontend
npm ci
npm run dev
```

Открыть **http://127.0.0.1:5173**. Vite проксирует `/api` на backend:8000.
Настройка по умолчанию не требует frontend `.env`. Для другого API скопировать
`frontend/.env.example` в `frontend/.env` и установить `VITE_API_BASE_URL`
(с окончанием `/api`); при другом origin сервер должен разрешать CORS.
Секретный AI-ключ остаётся только в backend `.env`.

```bash
cd frontend
npm run build
npm run preview
```

Preview доступен на `http://127.0.0.1:4173` и также проксирует `/api` на :8000.
Для отдельного размещения `dist/` нужны SPA fallback на `index.html` и proxy `/api`
или корректный `VITE_API_BASE_URL` во время сборки. Проверена среда Node 26.9.0,
npm 11.19.1; Vite 7 требует Node 20.19+ или 22.12+.

### Проверки frontend

`npm run build` проходит: TypeScript + production build Vite.
`npm run test:e2e`: первоначально 4 passed; после QA — **6 passed** (см. выше).
Через Vite proxy подтверждён ответ реального backend с сохранённой карточкой.
Браузерные тесты запускаются при работающих backend:8000 и frontend:5173:

```bash
cd frontend
npm run test:e2e
```

Требуется установленный Google Chrome (`channel: 'chrome'` в конфигурации).
Playwright использует изолированный профиль. Проверяются реальный API, полный путь
бизнес → команда → ручное решение, пересчёт 100 → 80 при замене данных на TBD,
публикация и отклик при 0 баллов, фильтры/сортировка/пустой каталог, отказ сети
и сохранность ввода, отсутствие runtime errors и горизонтального overflow на 390px.
Тесты **создают демонстрационные задачи и предложения в текущей БД** и не удаляют их;
для изолированной проверки запускайте backend с отдельным `DATABASE_URL` и seed.
Снимки экранов создаются в `.impeccable/review/`.
Визуальная проверка Impeccable: исходное замечание по контрасту исправлено;
повторный verdict `ship` подтверждает устранение именно этого замечания.
Отчёт — `.impeccable/review/finish-review.md`. Проверены девять снимков,
включая desktop 1440px и mobile 390px.

До QA frontend-прогон создал задачу **14** «Реестр заявок
мастерской» (80 / ready), одно принятое и одно отклонённое предложение;
задачу **15** с нулевым рейтингом и ожидающим решения предложением.
Для быстрого показа: `/tasks/14`, `/tasks/14/proposals`, `/tasks/15`.
При новой базе ID отличаются — используйте каталог.

### Ограничения frontend

- Переключение роли — демонстрация, не авторизация. Доступ к чужим задачам сервером
  не ограничен; не использовать для публичного продакшена.
- API не отдаёт «мои задачи», поэтому «Рабочие задачи» хранит только ID задач,
  созданных в этом браузере. Очистка localStorage удаляет эти ссылки, но не задачи БД.
- Поле industry в API отсутствует: на карточках используется реальный `topic`.
  Тема фильтруется точным совпадением без учёта регистра.
- Счётчики предложений собираются из существующего API предложений (до четырёх
  параллельных запросов), без нового backend endpoint. Подходит для масштаба MVP.
- Опубликованная карточка недоступна для правок согласно текущему контракту.
  Решение по предложению окончательное; максимум одна принятая команда.
- Внешний AI не включался и не проверялся живым ключом в frontend-сессии.
  Демонстрация полностью работает с существующим mock/fallback backend.
- README и презентация в этой сессии не изменялись.

Ниже сохранены сведения предыдущего backend-этапа.

## Что реализовано

1. Draft из короткого описания с AI/mock-анализом.
2. Недостающие поля и минимум три вопроса (API выдаёт ровно три).
3. Structured card из описания/ответов, частичное редактирование.
4. Ручное подтверждение и публикация, без порога рейтинга.
5. Рейтинг 0–100 сразу после создания, ответов и редактирования; breakdown,
   missing_fields, recommendations; GET /api/tasks/{id}/rating.
6. Каталог: рейтинг по возрастанию/убыванию, newest, фильтры topic и level.
7. Предложения: team_name, solution_idea, plan, estimated_duration, prototype_url,
   необязательный contact. При создании всегда pending.
8. Только ручные accepted/rejected; одно принятое предложение на задачу.
9. OpenAI Responses adapter, Pydantic-валидация, проверка источников и offline fallback.
10. Неразрушающая SQLite-миграция v1 → v2, дополненные seed-данные и тесты.

## Важные изменения контракта

- Рейтинг больше не null до подтверждения и не исчезает после редактирования.
  После правки подтверждение сбрасывается, а рейтинг пересчитывается сразу.
- Добавлен Task.rating с score, level, breakdown (массив компонентов), missing_fields,
  recommendations. Старые readiness_score/readiness_level/score_breakdown сохранены.
- Сортировка каталога по умолчанию rating_desc; topic — явная тема пользователя,
  нормализованная trim/casefold, а не AI-классификация.
- POST предложения теперь требует solution_idea, plan, estimated_duration вместо
  старого message. Старый формат POST возвращает 422. В ответе message сохранён.
- Низкий балл, включая 0, не блокирует публикацию и отклик.
- Явно пустые ответы считаются ответами для шага подтверждения, но баллов не дают.

Полный контракт, параметры, примеры и ошибки: [API_CONTRACT.md](API_CONTRACT.md).
Бизнес-правила и допущения: [SPEC.md](SPEC.md).

## AI и запрет на выдумывание фактов

TaskAI.prepare выполняет извлечение полей и выбор приоритетов уточнения.
Внешней модели разрешены только ссылки field/source_id на целые исходные фрагменты.
Свободного текста для заполнения карточки в схеме ответа AI нет.
Сервер берёт значения из пользовательского ввода; недопустимые ссылки/дубли/
структуры отклоняет. Явные метки, ответы и ручные правки нельзя переопределять.
Вопросы выводятся из нейтральных шаблонов, чтобы не внедрять выдуманные предпосылки.

AI_PROVIDER=mock по умолчанию: без сети/ключа, извлечение явно размеченных полей,
свободный текст сохраняется в context. AI_PROVIDER=openai включает Responses API
только при создании и сохранении ответов. Модель — OPENAI_MODEL, default gpt-4o-mini.

Если ключа нет либо AI отвечает с ошибкой, неверным JSON, отказом, неполным ответом
или неподтверждёнными ссылками, запрос выполняется с fallback. Провайдер и причина
видны в ai_analysis. Ключ/текст запроса не логируются. Автоповторов запросов нет,
таймаут HTTP-операций 12 секунд. Рейтинг всегда считает backend, не AI.

Важно: внешний API в этой сессии реально не вызывался — ключ не использовался.
HTTP-адаптер, корректный разбор и отказоустойчивость проверены через MockTransport.
Живой демонстрационный сценарий проверен с mock. Семантическая классификация
фрагмента моделью может ошибаться, поэтому ручная проверка карточки обязательна.

## Файлы

- backend/app/main.py — дополненные маршруты, сортировка, фильтры, ручные решения.
- backend/app/models.py — topic, ai_analysis и новые поля Proposal.
- backend/app/schemas.py — новый контракт, рейтинг, URL и валидация входных данных.
- backend/app/services.py — заполненность, рейтинг, рекомендации и вопросы.
- backend/app/ai/{service,providers,schemas}.py — AI-функция, адаптер и проверка output.
- database/migrate.py — транзакционное обновление SQLite по PRAGMA user_version.
- database/seed.py, database/seed_data.json — идемпотентный seed с новыми полями.
- backend/tests/{test_api,test_scenario_v2,test_ai,test_migration}.py,
  backend/tests/conftest.py — проверки; внешние запросы отключены по умолчанию.
- .env.example, README.md, docs/SPEC.md, docs/API_CONTRACT.md, docs/HANDOFF.md — обновлены.

Новые зависимости не потребовались; использован уже установленный httpx.
На backend-этапе frontend не изменялся; теперь реализован (см. выше).
Git-репозитория в рабочей директории нет, коммиты не создавались.

## SQLite и демонстрационные данные

При запуске, seed или python -m database.migrate выполняется идемпотентное обновление:
новые колонки добавляются, таблицы не удаляются. Старый message копируется в
solution_idea. Неизвестные plan, estimated_duration, prototype_url, topic остаются
пустыми. Производные рейтинги пересчитываются, пользовательский updated_at сохраняется.

Перед обновлением сделана database/app.pre-v2.db — резервная копия исходной v1.
Рабочая база — database/app.db. Обе исключены из git.
Сравнение с резервной копией подтвердило сохранность пользовательских данных всех
5 исходных задач и 4 предложений; изменялись только производные поля рейтинга.

В seed теперь 8 задач и 5 предложений. На существующей БД добавились три задачи:

| ID | Задача | Тема | Состояние / рейтинг |
| --- | --- | --- | --- |
| 6 | Отчёт о посещаемости | аналитика | published / 70 ready |
| 7 | Реестр оборудования | автоматизация | published / 100 priority |
| 8 | Тема пока не определена | образование | draft / 0 draft |

Повторный seed: 0 задач / 0 предложений. Существующие записи не перезаписываются;
у исходных задач после миграции topic остаётся пустым. В новой БД все seed-задачи
получат темы из seed_data.json.

Пример из API_CONTRACT.md дополнительно создал через настоящий HTTP задачу ID 9
(«Отчёт о посещаемости», published / 70 ready) и предложения ID 6 accepted, ID 7 rejected.
Итого после backend-проверки было 9 задач, 7 предложений; 6 задач опубликованы.
Браузерные frontend-тесты добавляют свои демонстрационные записи.
ID приведены для текущей БД, клиенты должны брать ID из ответов API.

## Результаты проверки

Среда: Python 3.9.6, зависимости из backend/requirements.txt.

| Проверка | Результат |
| --- | --- |
| python -m pytest backend/tests -q | 66 passed |
| python -m pip check | No broken requirements found |
| Миграция старой БД, повторный запуск | Успешно, данные и исходные timestamps сохранены |
| Повторный seed | added_tasks=0, added_proposals=0 |
| PRAGMA integrity_check | ok |
| PRAGMA foreign_key_check | Нарушений нет |
| Uvicorn 127.0.0.1:8000 | Application startup complete |
| GET /health | 200, status=ok |
| GET /docs, GET /openapi.json | 200 |
| Исполняемый пример API_CONTRACT через HTTP | Создание → вопросы → ответы → правка → 70 ready → confirm → publish → фильтры → proposals → accept/reject: успешно |

Автотесты покрывают все веса и границы рейтинга, заглушки, пересчёт после правок,
публикацию и отклик при 0 баллов, фильтры/сортировку/пагинацию, валидацию URL,
запрет автопринятия команды, повторные/окончательные решения, сохранность после
перезапуска, seed и миграцию. AI-тесты покрывают некорректный JSON, лишние поля,
выдуманные источники, перенос/потерю явных фактов, сохранение отрицаний и правок,
отсутствие ключа, timeout, HTTP 429/500, отказ и неполные ответы провайдера.

Окружение sandbox блокировало локальные HTTP-соединения; после разрешённого запуска
вне sandbox проверки прошли. Это ограничение окружения, не ошибка приложения.
Сервер последней версии оставлен запущенным на http://127.0.0.1:8000.

## Как запустить

Из корня проекта (окружение .venv уже подготовлено):

```bash
source .venv/bin/activate
python -m database.seed
python -m uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000
```

Для чистой установки сначала python3 -m venv .venv, затем активация и
python -m pip install -r backend/requirements.txt. Миграция вызывается автоматически;
вручную — python -m database.migrate. Относительный DATABASE_URL считается от корня.
Если порт 8000 занят оставленным сервером, используйте его либо остановите перед новым запуском.

```bash
python -m pytest backend/tests -q
curl http://127.0.0.1:8000/health
```

Для внешнего AI настройте локальный .env по .env.example, задайте AI_PROVIDER=openai,
OPENAI_API_KEY и при необходимости OPENAI_MODEL, затем перезапустите сервер.
Появление ключа само по себе не включает внешнюю отправку данных.

## Сохраняющиеся ограничения

- Нет авторизации/проверки владельцев: только доверенная локальная демонстрация.
- Опубликованная карточка не редактируется; нет отмены публикации и пересмотра решения.
- Семантика заполненности проверяется только списком заглушек; произвольный текст
  может получать баллы. Рейтинг не является оценкой достоверности или качества.
- Нет версионирования конкурентных правок карточки, дедупликации команд и загрузки файлов.
- У новой схемы есть только переход v1 → v2, не общий фреймворк миграций.
- Frontend реализован отдельным этапом без изменения backend; см. начало документа.
